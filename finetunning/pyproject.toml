[project]
name = "finetunning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.13.0",
    "bitsandbytes>=0.49.2",
    "transformers>=5.4.0",
    "celery",
    "redis",
    "python-dotenv>=1.2.2",
]
